[package]
name = "nexora_bootloader"
version = "0.1.0"
edition = "2024"
authors = ["NEXORA OS Project"]
license = "MIT"
description = "Bootloader for the NEXORA OS mobile operating system."

[dependencies]
